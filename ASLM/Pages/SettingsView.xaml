<!-- Copyright NGGT.LightKeeper. All Rights Reserved. -->

<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ASLM.Pages.SettingsView"
             BackgroundColor="#40000000">

    <ContentView.GestureRecognizers>
        <TapGestureRecognizer Tapped="OnBackgroundTapped" />
    </ContentView.GestureRecognizers>

    <Border x:Name="SettingsDialog"
            WidthRequest="1024"
            HeightRequest="576"
            HorizontalOptions="Center"
            VerticalOptions="Center"
            BackgroundColor="{StaticResource BackgroundSecondary}"
            Stroke="{StaticResource Separator}"
            StrokeThickness="1"
            Padding="0"
            StrokeShape="RoundRectangle 8">

        <Border.GestureRecognizers>
            <TapGestureRecognizer Tapped="OnBorderTapped" />
        </Border.GestureRecognizers>

        <Grid ColumnDefinitions="220,*" ColumnSpacing="0">

            <Border Grid.Column="0"
                    BackgroundColor="#18181A"
                    StrokeThickness="0"
                    StrokeShape="RoundRectangle 0"
                    Margin="12"
                    Padding="0">
                <Grid RowDefinitions="Auto,*">
                    <Label Text="Settings"
                           FontSize="16"
                           FontAttributes="Bold"
                           TextColor="{StaticResource LabelPrimary}" />

                    <ScrollView x:Name="CategoryScroll"
                                Grid.Row="1"
                                Margin="0,12,0,0"
                                VerticalScrollBarVisibility="Never">
                                <VerticalStackLayout x:Name="CategoryPanel" Spacing="1" />
                    </ScrollView>
                </Grid>
            </Border>

            <BoxView Grid.Column="0"
                     HorizontalOptions="End"
                     WidthRequest="1"
                     Color="{StaticResource Separator}"
                     Opacity="0.5" />

            <Grid Grid.Column="1"
                  RowDefinitions="Auto,*,Auto"
                  RowSpacing="0"
                  Margin="12"
                  Padding="0">

                <Grid Grid.Row="0"
                      ColumnDefinitions="*,Auto"
                      Margin="0"
                      Padding="0"
                      HeightRequest="50">
                    <Label x:Name="ActiveCategoryTitleLabel"
                           Text=""
                           FontSize="16"
                           FontAttributes="Bold"
                           TextColor="{StaticResource LabelPrimary}"
                           VerticalOptions="Start" />

                    <Button Grid.Column="1"
                            Text="&#x2715;"
                            FontSize="17"
                            WidthRequest="36"
                            MinimumWidthRequest="36"
                            HeightRequest="36"
                            MinimumHeightRequest="36"
                            Padding="0"
                            Margin="0"
                            ToolTipProperties.Text="Close Settings"
                            BackgroundColor="Transparent"
                            TextColor="{StaticResource LabelSecondary}"
                            CornerRadius="5"
                            VerticalOptions="Start"
                            Clicked="OnCloseClicked" />
                </Grid>

                <ScrollView Grid.Row="1"
                            x:Name="SettingsScroll"
                            Margin="0"
                            VerticalScrollBarVisibility="Never"
                            HorizontalScrollBarVisibility="Never">
                    <VerticalStackLayout x:Name="SettingsContentContainer"
                                         Spacing="16"
                                         Padding="0">

                        <VerticalStackLayout x:Name="AslmSettingsContainer"
                                             Spacing="0"
                                             IsVisible="False">
                            <VerticalStackLayout x:Name="UserProfileSection" Spacing="8">
                                <Label Text="Display Name"
                                       FontSize="14"
                                       FontAttributes="Bold"
                                       TextColor="{StaticResource LabelPrimary}" />
                                <Label Text="Name shown in ASLM and shared with modules."
                                       FontSize="12"
                                       TextColor="{StaticResource LabelSecondary}" />
                                <Border BackgroundColor="#19191C"
                                        Stroke="{StaticResource Separator}"
                                        StrokeThickness="1"
                                        StrokeShape="RoundRectangle 5"
                                        Padding="8,0">
                                    <Entry x:Name="UsernameEntry"
                                           FontSize="13"
                                           HeightRequest="40"
                                           TextColor="{StaticResource LabelPrimary}"
                                           BackgroundColor="Transparent" />
                                </Border>
                            </VerticalStackLayout>

                            <VerticalStackLayout x:Name="PortsSection" Spacing="8">
                                <Label x:Name="PortsGroupHeader"
                                       Text="Ports"
                                       FontSize="14"
                                       FontAttributes="Bold"
                                       TextColor="{StaticResource LabelTertiary}"
                                       Margin="0" />
                                <VerticalStackLayout Spacing="8">
                                    <Label Text="Official modules start port"
                                           FontSize="14"
                                           FontAttributes="Bold"
                                           TextColor="{StaticResource LabelPrimary}" />
                                    <Label Text="Starting point for official modules. 100 ports are reserved from this value."
                                           FontSize="12"
                                           TextColor="{StaticResource LabelSecondary}" />
                                    <Border BackgroundColor="#19191C"
                                            Stroke="{StaticResource Separator}"
                                            StrokeThickness="1"
                                            StrokeShape="RoundRectangle 4"
                                            Padding="8,0">
                                        <Entry x:Name="OfficialPortEntry"
                                               Keyboard="Numeric"
                                               FontSize="13"
                                               HeightRequest="40"
                                               TextColor="{StaticResource LabelPrimary}"
                                               BackgroundColor="Transparent" />
                                    </Border>
                                </VerticalStackLayout>

                                <VerticalStackLayout Spacing="8">
                                    <Label Text="Third-party modules start port"
                                           FontSize="14"
                                           FontAttributes="Bold"
                                           TextColor="{StaticResource LabelPrimary}" />
                                    <Label Text="Starting point for external modules. 1000 ports are reserved from this value."
                                           FontSize="12"
                                           TextColor="{StaticResource LabelSecondary}" />
                                    <Border BackgroundColor="#19191C"
                                            Stroke="{StaticResource Separator}"
                                            StrokeThickness="1"
                                            StrokeShape="RoundRectangle 4"
                                            Padding="8,0">
                                        <Entry x:Name="ThirdPartyPortEntry"
                                               Keyboard="Numeric"
                                               FontSize="13"
                                               HeightRequest="40"
                                               TextColor="{StaticResource LabelPrimary}"
                                               BackgroundColor="Transparent" />
                                    </Border>
                                </VerticalStackLayout>

                                <Label x:Name="PortErrorLabel"
                                       Text=""
                                       FontSize="12"
                                       TextColor="{StaticResource ActionRed}"
                                       IsVisible="False" />
                            </VerticalStackLayout>
                        </VerticalStackLayout>

                        <VerticalStackLayout x:Name="ModuleSettingsContainer"
                                             Spacing="0"
                                             IsVisible="False" />

                        <VerticalStackLayout x:Name="EmptyCategoryState"
                                             IsVisible="False"
                                             Padding="0">
                            <Label x:Name="EmptyCategoryLabel"
                                   Text="This category has no settings."
                                   FontSize="13"
                                   TextColor="{StaticResource LabelSecondary}" />
                        </VerticalStackLayout>

                    </VerticalStackLayout>
                </ScrollView>

                <Grid Grid.Row="2"
                      RowDefinitions="Auto,Auto"
                      Margin="0,8,0,0">
                    <BoxView HeightRequest="1"
                             Color="{StaticResource Separator}"
                             Opacity="0.45" />

                    <Grid Grid.Row="1"
                          ColumnDefinitions="Auto,Auto,*,Auto,Auto"
                          ColumnSpacing="6"
                          Margin="0,10,0,0">
                        <Button x:Name="DefaultButton"
                                Text="Load default"
                                Style="{StaticResource SettingsFooterButtonStyle}"
                                IsVisible="False"
                                Clicked="OnDefaultClicked" />

                        <Button x:Name="DiscardButton"
                                Grid.Column="1"
                                Text="Discard Changes"
                                Style="{StaticResource SettingsFooterDangerButtonStyle}"
                                IsVisible="False"
                                Clicked="OnDiscardChangesClicked" />

                        <Button x:Name="SaveButton"
                                Grid.Column="3"
                                Text="Save"
                                Style="{StaticResource SettingsFooterButtonStyle}"
                                IsVisible="False"
                                Clicked="OnSaveClicked" />

                        <Button x:Name="SaveAndRestartButton"
                                Grid.Column="4"
                                Text="Save and restart"
                                Style="{StaticResource SettingsFooterPrimaryButtonStyle}"
                                IsVisible="False"
                                Clicked="OnSaveAndRestartClicked" />
                    </Grid>
                </Grid>
            </Grid>
        </Grid>
    </Border>

</ContentView>
