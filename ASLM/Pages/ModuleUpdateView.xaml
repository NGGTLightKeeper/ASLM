<!-- Copyright NGGT.LightKeeper. All Rights Reserved. -->

<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:services="clr-namespace:ASLM.Services"
             x:Class="ASLM.Pages.ModuleUpdateView"
             x:Name="RootView"
             BackgroundColor="{DynamicResource OverlayBackground}">

    <ContentView.GestureRecognizers>
        <TapGestureRecognizer Tapped="OnBackgroundTapped" />
    </ContentView.GestureRecognizers>

    <Border x:Name="DialogBorder"
            WidthRequest="940"
            HeightRequest="620"
            HorizontalOptions="Center"
            VerticalOptions="Center"
            BackgroundColor="{DynamicResource BackgroundSecondary}"
            Stroke="{DynamicResource Separator}"
            StrokeThickness="1"
            Padding="0"
            StrokeShape="RoundRectangle 10">

        <Border.GestureRecognizers>
            <TapGestureRecognizer Tapped="OnDialogTapped" />
        </Border.GestureRecognizers>

        <Grid RowDefinitions="Auto,*,Auto"
              RowSpacing="0"
              Padding="12">

            <Grid Grid.Row="0"
                  ColumnDefinitions="*,Auto"
                  Margin="0,0,0,8">
                <VerticalStackLayout Spacing="4">
                    <Label Text="{Binding DialogTitle, Source={x:Reference RootView}}"
                           FontSize="20"
                           FontAttributes="Bold"
                           TextColor="{DynamicResource LabelPrimary}" />
                    <Label Text="{Binding DialogSubtitle, Source={x:Reference RootView}}"
                           FontSize="14"
                           TextColor="{DynamicResource LabelSecondary}"
                           MaxLines="2"
                           LineBreakMode="TailTruncation" />
                </VerticalStackLayout>

                <Button x:Name="CloseButton"
                        Grid.Column="1"
                        Text="&#x2715;"
                        FontSize="17"
                        WidthRequest="34"
                        HeightRequest="34"
                        Padding="0"
                        Margin="0"
                        ToolTipProperties.Text="Close module updates"
                        BackgroundColor="Transparent"
                        TextColor="{DynamicResource LabelSecondary}"
                        CornerRadius="6"
                        VerticalOptions="Center"
                        Clicked="OnCloseClicked" />
            </Grid>

            <Grid Grid.Row="1"
                  RowDefinitions="Auto,Auto,*"
                  RowSpacing="8"
                  Padding="0">
                    <Border Grid.Row="0"
                            BackgroundColor="{DynamicResource BackgroundTertiary}"
                            Stroke="{DynamicResource Separator}"
                            StrokeThickness="1"
                            StrokeShape="RoundRectangle 6"
                            Padding="8">
                        <Grid ColumnDefinitions="*,*"
                              ColumnSpacing="8">
                            <VerticalStackLayout Spacing="4">
                                <Label x:Name="CurrentVersionHeaderLabel"
                                       Text="Current version"
                                       FontSize="13"
                                       TextColor="{DynamicResource LabelSecondary}" />
                                <Label Text="{Binding CurrentVersionLabel, Source={x:Reference RootView}}"
                                       FontSize="14"
                                       FontAttributes="Bold"
                                       TextColor="{DynamicResource LabelPrimary}" />
                            </VerticalStackLayout>

                            <VerticalStackLayout Grid.Column="1"
                                                 Spacing="4">
                                <Label x:Name="SelectedTargetHeaderLabel"
                                       Text="Selected target"
                                       FontSize="13"
                                       TextColor="{DynamicResource LabelSecondary}" />
                                <Label Text="{Binding TargetVersionLabel, Source={x:Reference RootView}}"
                                       FontSize="14"
                                       FontAttributes="Bold"
                                       TextColor="{DynamicResource LabelPrimary}" />
                            </VerticalStackLayout>
                        </Grid>
                    </Border>

                    <Border Grid.Row="1"
                            BackgroundColor="{DynamicResource BackgroundTertiary}"
                            Stroke="{DynamicResource Separator}"
                            StrokeThickness="0"
                            StrokeShape="RoundRectangle 0"
                            Padding="0,8,0,0">
                        <VerticalStackLayout Spacing="8">
                            <VerticalStackLayout Spacing="4">
                                <Label x:Name="UpdateSourceHeaderLabel"
                                       Text="Update source"
                                       FontSize="16"
                                       FontAttributes="Bold"
                                       TextColor="{DynamicResource LabelPrimary}" />
                                <Label x:Name="PrefsAutoSavedLabel"
                                       Text="Preferences are saved automatically."
                                       FontSize="13"
                                       TextColor="{DynamicResource LabelSecondary}" />
                            </VerticalStackLayout>

                            <Grid ColumnDefinitions="*,*"
                                  ColumnSpacing="12">
                                <VerticalStackLayout Spacing="6">
                                    <Label x:Name="SearchModeLabel"
                                           Text="Search mode"
                                           FontSize="13"
                                           TextColor="{DynamicResource LabelSecondary}" />
                                    <Border BackgroundColor="{DynamicResource BackgroundSecondary}"
                                            Stroke="{DynamicResource Separator}"
                                            StrokeThickness="1"
                                            StrokeShape="RoundRectangle 6"
                                            Padding="8,0">
                                        <Picker x:Name="SourceModePicker"
                                                ItemsSource="{Binding SourceModeOptions, Source={x:Reference RootView}}"
                                                SelectedItem="{Binding SelectedSourceMode, Source={x:Reference RootView}, Mode=OneWay}"
                                                SelectedIndexChanged="OnSourceModeSelectionChanged"
                                                FontSize="13"
                                                HeightRequest="44"
                                                BackgroundColor="Transparent"
                                                TextColor="{DynamicResource LabelPrimary}"
                                                TitleColor="{DynamicResource LabelSecondary}" />
                                    </Border>
                                </VerticalStackLayout>

                                <VerticalStackLayout Grid.Column="1"
                                                     Spacing="6"
                                                     IsVisible="{Binding IsReleaseMode, Source={x:Reference RootView}}">
                                    <Label x:Name="ReleaseVersionLabel"
                                           Text="Release version"
                                           FontSize="13"
                                           TextColor="{DynamicResource LabelSecondary}" />
                                    <Border BackgroundColor="{DynamicResource BackgroundSecondary}"
                                            Stroke="{DynamicResource Separator}"
                                            StrokeThickness="1"
                                            StrokeShape="RoundRectangle 8"
                                            Padding="12,0">
                                        <Picker x:Name="ReleasePicker"
                                                ItemsSource="{Binding ReleaseOptions, Source={x:Reference RootView}}"
                                                SelectedItem="{Binding SelectedReleaseOption, Source={x:Reference RootView}, Mode=OneWay}"
                                                SelectedIndexChanged="OnReleaseSelectionChanged"
                                                ItemDisplayBinding="{Binding DisplayName}"
                                                FontSize="13"
                                                HeightRequest="44"
                                                BackgroundColor="Transparent"
                                                TextColor="{DynamicResource LabelPrimary}"
                                                TitleColor="{DynamicResource LabelSecondary}" />
                                    </Border>
                                </VerticalStackLayout>

                                <VerticalStackLayout Grid.Column="1"
                                                     Spacing="6"
                                                     IsVisible="{Binding IsBranchMode, Source={x:Reference RootView}}">
                                    <Label x:Name="RepositoryBranchLabel"
                                           Text="Repository branch"
                                           FontSize="12"
                                           TextColor="{DynamicResource LabelSecondary}" />
                                    <Border BackgroundColor="{DynamicResource BackgroundSecondary}"
                                            Stroke="{DynamicResource Separator}"
                                            StrokeThickness="1"
                                            StrokeShape="RoundRectangle 6"
                                            Padding="8,0">
                                        <Picker x:Name="BranchPicker"
                                                ItemsSource="{Binding BranchOptions, Source={x:Reference RootView}}"
                                                SelectedIndexChanged="OnBranchSelectionChanged"
                                                FontSize="13"
                                                HeightRequest="44"
                                                BackgroundColor="Transparent"
                                                TextColor="{DynamicResource LabelPrimary}"
                                                TitleColor="{DynamicResource LabelSecondary}" />
                                    </Border>
                                </VerticalStackLayout>
                            </Grid>
                        </VerticalStackLayout>
                    </Border>

                    <Border Grid.Row="2"
                            BackgroundColor="{DynamicResource BackgroundTertiary}"
                            Stroke="{DynamicResource Separator}"
                            StrokeThickness="0"
                            StrokeShape="RoundRectangle 0"
                            Padding="0,8,0,0"
                            VerticalOptions="Fill">
                        <Grid RowDefinitions="Auto,Auto,Auto,Auto,Auto,*"
                              RowSpacing="1">
                            <VerticalStackLayout Spacing="4">
                                <Label Text="{Binding ActivityTitle, Source={x:Reference RootView}}"
                                       FontSize="16"
                                       FontAttributes="Bold"
                                       TextColor="{DynamicResource LabelPrimary}" />
                                <Label Text="{Binding ActivityStatus, Source={x:Reference RootView}}"
                                       FontSize="13"
                                       TextColor="{DynamicResource LabelSecondary}"
                                       MaxLines="2"
                                       LineBreakMode="TailTruncation" />
                            </VerticalStackLayout>

                            <Label x:Name="OverallProgressLabel"
                                   Grid.Row="1"
                                   Text="Overall progress"
                                   FontSize="10"
                                   TextColor="{DynamicResource LabelSecondary}"
                                   IsVisible="{Binding HasActivityProgress, Source={x:Reference RootView}}" />

                            <ProgressBar Grid.Row="2"
                                         Progress="{Binding OverallProgress, Source={x:Reference RootView}}"
                                         ProgressColor="{DynamicResource ActionBlue}"
                                         HeightRequest="6"
                                         IsVisible="{Binding HasActivityProgress, Source={x:Reference RootView}}" />

                            <Label Grid.Row="3"
                                   Text="{Binding DownloadDetail, Source={x:Reference RootView}}"
                                   FontSize="10"
                                   TextColor="{DynamicResource LabelSecondary}"
                                   IsVisible="{Binding HasDownloadDetail, Source={x:Reference RootView}}" />

                            <ProgressBar Grid.Row="4"
                                         Progress="{Binding FileProgress, Source={x:Reference RootView}}"
                                         ProgressColor="{DynamicResource ActionGreen}"
                                         HeightRequest="4"
                                         IsVisible="{Binding HasActivityProgress, Source={x:Reference RootView}}" />

                            <services:ConsoleOutputView x:Name="UpdateLogConsole"
                                                        Grid.Row="5"
                                                        IsVisible="{Binding HasLog, Source={x:Reference RootView}}"
                                                        Text="{Binding LogText, Source={x:Reference RootView}}"
                                                        SessionKey="{Binding UpdateLogSessionKey, Source={x:Reference RootView}}"
                                                        HorizontalOptions="Fill"
                                                        VerticalOptions="Fill"
                                                        Margin="0,4,0,0" />
                        </Grid>
                    </Border>
            </Grid>

            <Grid Grid.Row="2"
                  ColumnDefinitions="*,Auto,Auto,Auto"
                  ColumnSpacing="8"
                  Margin="0,14,0,0">
                <ActivityIndicator Grid.Column="1"
                                   IsVisible="{Binding IsBusy, Source={x:Reference RootView}}"
                                   IsRunning="{Binding IsBusy, Source={x:Reference RootView}}"
                                   Color="{DynamicResource LabelPrimary}"
                                   VerticalOptions="Center" />

                <Button x:Name="CheckUpdatesButton"
                        Grid.Column="2"
                        Text="Check updates"
                        HeightRequest="36"
                        MinimumHeightRequest="36"
                        CornerRadius="6"
                        Padding="8,0"
                        BackgroundColor="{DynamicResource SystemGray4}"
                        TextColor="{DynamicResource LabelPrimary}"
                        IsEnabled="{Binding CanCheckUpdates, Source={x:Reference RootView}}"
                        Clicked="OnCheckUpdatesClicked" />

                <Button x:Name="InstallUpdateButton"
                        Grid.Column="3"
                        Text="Install update"
                        HeightRequest="36"
                        MinimumHeightRequest="36"
                        CornerRadius="6"
                        Padding="8,0"
                        BackgroundColor="{DynamicResource ActionBlue}"
                        TextColor="{DynamicResource White}"
                        IsVisible="{Binding ShowInstallAction, Source={x:Reference RootView}}"
                        IsEnabled="{Binding CanInstallUpdate, Source={x:Reference RootView}}"
                        Clicked="OnInstallUpdateClicked" />
            </Grid>
        </Grid>
    </Border>

</ContentView>
